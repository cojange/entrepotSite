<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.site.admin.magazine.adMagazine.dao.AdMagazineDao">
	<select id="adMagazineList" parameterType="msvo" resultType="msvo">
		/*상품 리스트*/
		select pd_cost, mg_name, pl_path, mg_num
		from magazine_list_view
		order by SUBSTR(reverse(substr(reverse(pl_path), 1, instr(reverse(pl_path), '_') - 1)),1,instr(reverse(substr(reverse(pl_path), 1, instr(reverse(pl_path), '_') - 1)),'.')-1) desc
	</select>
</mapper>