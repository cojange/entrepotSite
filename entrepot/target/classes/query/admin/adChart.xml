<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.site.admin.chart.adChart.dao.AdChartDao">
	<select id="columnChartData" parameterType="int" resultType="ccvo">
		select to_char(sell_date,'mm') category, sum(chartdata) rowM from columnChartData
		<where>
			<if test="chartData>=0">
				<![CDATA[sell_date > add_months(sysdate,#{selectmonth})]]>
			</if>
		</where>
		group by to_char(sell_date,'mm')
	</select>
</mapper>